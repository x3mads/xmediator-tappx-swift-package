// Intentionally empty.
// This wrapper target exists so the XMediatorTappx product can combine the
// XMediatorTappx binary target and carry their required
// system-framework link settings (binary targets cannot declare either).
